# Reusable Liferay Assets Registry

This file contains a list of GitHub repositories that the AI agent should check for reusable Fragments and Client Extensions before building new components from scratch.

## Source Repositories


## Discovery Instructions
1.  **Search Path:** Look primarily in `liferay/fragments/` and `liferay/client-extensions/` (or equivalent paths for older workspace versions).
2.  **Filter:** Only utilize assets that match your use case.
3.  **Adaptation:** When an asset is found, check if any modifications are necessary. If so, propose how it should be modified to comply with the global mandates in `GEMINI.md` (e.g., updating CSS variables for the Classic theme).
